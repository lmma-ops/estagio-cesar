[pytest]
testpaths = Tests 
adopts = --html=report.html --self-contained-html --alluredir=allure-results
markers = 
    smoke: mark a test as a smoke test.
    regression: mark a test as a regression test.
